用时四天零一个半小时
仍有一些小问题待解决
当object=undefined时  object！=null可以 object！==null不行
因为在js中 undefined==null是true 但是undefined===null是false
indexof(strb);和lastindexof(stra)
输入B中的内容，在输入A的内容中第一次出现的位置（indexOf）</button>
输入A中的内容，在输入B的内容中最后一次出现的位置（lastIndexOf）
还有当前选中输入框的行数
 p.innerHTML=stra.split(/\r?\n/).length;仍未实现
